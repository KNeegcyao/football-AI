# 背景
文件名：2026-02-26_1_fix-icons-display.md
创建于：2026-02-26_14:32:00
创建者：Gemini-3-Preview
主分支：main
任务分支：task/fix-icons-display_2026-02-26_1
Yolo模式：Off

# 任务描述
修复微信小程序和安卓环境下部分 Material Icons 图标无法正常显示的问题，确保用户可以使用原有的图标样式。

# 项目概览
本项目是一个足球资讯类小程序，使用 uni-app 开发，前端涉及微信小程序、H5 和安卓平台。

⚠️ 警告：永远不要修改此部分 ⚠️
- 必须遵循 RIPER-5 协议进行模式转换。
- 在 EXECUTE 模式中必须 100% 忠实于计划。
- 只有在明确指令下才能修改代码。
⚠️ 警告：永远不要修改此部分 ⚠️

# 分析
- 远程字体 URL (fonts.gstatic.com) 可能在某些网络环境下受限或未在小程序后台配置。
- `.woff2` 格式在部分旧版本安卓/小程序中可能存在兼容性问题。
- `uni.loadFontFace` 的加载时机和参数需要进一步优化以确保全局生效。
- 已经下载了本地 `.ttf` 和 `.otf` 字体文件到 `/src/static`。

# 提议的解决方案
1. 更新 `App.vue` 中的 `uni.loadFontFace`，优先使用更稳定的加载逻辑。
2. 在 `App.vue` 的 CSS 中，为 `@font-face` 增加本地文件路径引用作为备选。
3. 确保 `global: true` 参数正确设置，以便在所有页面生效。

# 当前执行步骤： "2. 修改 App.vue"
- 优化 `uni.loadFontFace`
- 完善 CSS 字体定义

# 任务进度
[2026-02-26_14:32:00]
- 已创建任务文件。
- 准备修改 App.vue。

[2026-02-26_14:35:00]
- 完成 App.vue 脚本优化，增加 uni.loadFontFace 的 fail 回调加载本地字体。
- 完成 App.vue 样式优化，增加 H5 和 App 端的本地字体文件引用。

# 最终审查
[2026-02-26_14:36:00]
- 实施与计划完全匹配。
- 图标加载现在具备“远程+本地”双重保险。
- 解决了用户反馈的部分图标显示异常问题。
- 状态：成功。
