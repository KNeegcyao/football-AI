# 绿茵集后台管理系统 — 详细设计文档
> 本文档为完整复现指南，可在另一台电脑上通过 Trae 等 AI 编码工具逐章节输入，完整复现整个后台管理系统。
>

---

## 一、项目概览
### 1.1 系统定位
绿茵集（PitchPulse）后台管理系统是绿茵集智能足球社区平台的管理端，面向管理员提供数据监控、用户管理、内容审核等功能。采用简化设计，6 个核心页面即可体现完整的管理能力。

### 1.2 技术栈
| 层级 | 技术 | 版本 |
| --- | --- | --- |
| 前端框架 | Vue 3 (Composition API + `<script setup>`) | ^3.4.21 |
| UI 组件库 | Element Plus | ^2.7.0 |
| 构建工具 | Vite | ^5.2.8 |
| 状态管理 | Pinia | ^2.1.7 |
| 路由 | Vue Router 4 | ^4.3.0 |
| HTTP 请求 | Axios | ^1.6.0 |
| 后端框架 | Spring Boot | 3.2.5 |
| Java | JDK | 17 |
| ORM | MyBatis-Plus | 3.5.7 |
| 数据库 | MySQL | 8.x |
| 缓存 | Redis | — |
| 认证 | Spring Security + JWT (jjwt) | 0.12.5 |
| API 文档 | Knife4j (OpenAPI 3) | 4.5.0 |


### 1.3 项目结构
```plain
football-AI/
├── frontend/                          # 现有 Uni-app 前端（不修改）
├── soccer-forum-parent/               # 现有 Spring Boot 后端（新增管理接口）
│   ├── soccer-forum-common/
│   ├── soccer-forum-domain/
│   └── soccer-forum-service/
│       └── src/main/java/com/soccer/forum/service/
│           ├── config/                # SecurityConfig 新增管理端路径配置
│           ├── security/              # JWT 过滤器（已有，无需修改）
│           └── modules/
│               └── admin/             # ★ 新增：管理模块
│                   ├── controller/
│                   ├── service/
│                   ├── service/impl/
│                   ├── mapper/
│                   └── dto/
├── admin-frontend/                    # ★ 新增：后台管理前端项目
│   ├── public/
│   ├── src/
│   │   ├── api/                       # API 请求封装
│   │   ├── assets/                    # 静态资源
│   │   ├── components/                # 公共组件
│   │   ├── layout/                    # 布局组件
│   │   ├── router/                    # 路由配置
│   │   ├── stores/                    # Pinia 状态
│   │   ├── utils/                     # 工具函数
│   │   └── views/                     # 页面视图
│   │       ├── login/
│   │       ├── dashboard/
│   │       ├── user/
│   │       ├── post/
│   │       ├── comment/
│   │       ├── topic/
│   │       └── news/
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
└── docker/sql/init.sql                # 已有，含 admin 种子用户
```

---

## 二、后端设计
### 2.1 权限控制设计
#### 2.1.1 现有角色体系
项目中 `UserRole` 枚举已定义 `USER` 和 `ADMIN` 两个角色，`LoginUser` 中已将角色映射为 Spring Security 的 `ROLE_USER` 和 `ROLE_ADMIN`。但当前没有任何接口级别的角色校验。

#### 2.1.2 新增权限控制方案
**方案：在 SecurityConfig 中对管理端路径添加角色校验 + 使用 **`@PreAuthorize`** 注解**

1. **SecurityConfig 修改**（文件：`soccer-forum-service/.../config/SecurityConfig.java`）：

在 `SecurityFilterChain` 中新增管理端路径规则：

```java
// 管理端接口 — 需要 ADMIN 角色
.authorizeHttpRequests(auth -> auth
    .requestMatchers("/api/admin/**").hasRole("ADMIN")
    // ... 保留原有规则
)
```

2. **启用方法级安全**：在 SecurityConfig 类上添加注解：

```java
@EnableMethodSecurity
```

3. **Controller 方法级校验**：每个管理端 Controller 方法添加：

```java
@PreAuthorize("hasRole('ADMIN')")
```

#### 2.1.3 管理员登录
复用现有 `/api/auth/login` 接口，前端根据返回的用户角色（`role: "ADMIN"`）决定跳转到管理端还是用户端。无需新增登录接口。

---

### 2.2 管理端 API 设计
所有管理端接口统一前缀 `/api/admin`，返回格式复用 `R<T>` 通用响应体。

#### 2.2.1 数据看板接口
| 方法 | 路径 | 说明 | 请求参数 | 返回数据 |
| --- | --- | --- | --- | --- |
| GET | `/api/admin/dashboard/stats` | 获取核心统计数据 | 无 | `DashboardStatsVO` |
| GET | `/api/admin/dashboard/trend` | 获取近7天趋势数据 | `?days=7` | `List<DailyTrendVO>` |


**DashboardStatsVO：**

```java
public class DashboardStatsVO {
    private Long totalUsers;        // 总用户数
    private Long totalPosts;        // 总帖子数
    private Long totalComments;     // 总评论数
    private Long totalTopics;       // 总话题数
    private Long todayNewUsers;     // 今日新增用户
    private Long todayNewPosts;     // 今日新增帖子
    private Long todayNewComments;  // 今日新增评论
}
```

**DailyTrendVO：**

```java
public class DailyTrendVO {
    private String date;            // 日期 yyyy-MM-dd
    private Long newUsers;          // 当日新增用户
    private Long newPosts;          // 当日新增帖子
    private Long newComments;       // 当日新增评论
}
```

**实现要点：**

+ `totalUsers`: `SELECT COUNT(*) FROM users WHERE status = 1`
+ `totalPosts`: `SELECT COUNT(*) FROM posts WHERE status = 0`（0=正常）
+ `todayNewUsers`: `SELECT COUNT(*) FROM users WHERE DATE(created_time) = CURDATE() AND status = 1`
+ 趋势数据：按天 GROUP BY 统计近 N 天

---

#### 2.2.2 用户管理接口
| 方法 | 路径 | 说明 | 请求参数 | 返回数据 |
| --- | --- | --- | --- | --- |
| GET | `/api/admin/users` | 分页查询用户列表 | `?page=1&size=10&keyword=&role=&status=` | `PageResult<UserVO>` |
| PUT | `/api/admin/users/{id}/status` | 修改用户状态（启用/禁用） | Body: `{"status": 0或1}` | `R<Void>` |
| PUT | `/api/admin/users/{id}/role` | 修改用户角色 | Body: `{"role": "USER"或"ADMIN"}` | `R<Void>` |
| DELETE | `/api/admin/users/{id}` | 删除用户（逻辑删除） | 无 | `R<Void>` |


**UserVO（返回给管理端的用户视图）：**

```java
public class UserVO {
    private Long id;
    private String username;
    private String nickname;
    private String email;
    private String phone;
    private String avatar;
    private String role;            // USER / ADMIN
    private Integer status;         // 0=禁用, 1=正常
    private Integer level;
    private Integer experience;
    private LocalDateTime createdTime;
    private LocalDateTime updatedTime;
}
```

**实现要点：**

+ 分页查询：MyBatis-Plus `Page<User>` + 条件构造器，支持 keyword 模糊搜索 username/nickname
+ 修改状态：`UPDATE users SET status = ? WHERE id = ?`
+ 修改角色：`UPDATE users SET role = ? WHERE id = ?`
+ 删除用户：逻辑删除 `UPDATE users SET status = 0 WHERE id = ?`（不物理删除）

---

#### 2.2.3 帖子管理接口
| 方法 | 路径 | 说明 | 请求参数 | 返回数据 |
| --- | --- | --- | --- | --- |
| GET | `/api/admin/posts` | 分页查询帖子列表 | `?page=1&size=10&keyword=&status=&topicId=` | `PageResult<PostVO>` |
| PUT | `/api/admin/posts/{id}/status` | 修改帖子状态（显示/隐藏） | Body: `{"status": 0或1}` | `R<Void>` |
| DELETE | `/api/admin/posts/{id}` | 删除帖子（逻辑删除） | 无 | `R<Void>` |


**PostVO（管理端视图）：**

```java
public class PostVO {
    private Long id;
    private String title;
    private String content;         // 截断显示，最多200字
    private String authorName;      // 发帖用户昵称
    private Long authorId;
    private Integer views;
    private Integer likes;
    private Integer commentCount;
    private Integer status;         // 0=正常, 1=已删除
    private Long topicId;
    private String topicName;       // 话题名称
    private List<String> images;
    private LocalDateTime createdTime;
}
```

**实现要点：**

+ 列表查询关联用户表获取 authorName，关联话题表获取 topicName
+ content 截断：`content.substring(0, Math.min(200, content.length())) + "..."`
+ 逻辑删除：`UPDATE posts SET status = 1 WHERE id = ?`

---

#### 2.2.4 评论管理接口
| 方法 | 路径 | 说明 | 请求参数 | 返回数据 |
| --- | --- | --- | --- | --- |
| GET | `/api/admin/comments` | 分页查询评论列表 | `?page=1&size=10&keyword=&postId=` | `PageResult<CommentVO>` |
| DELETE | `/api/admin/comments/{id}` | 删除评论（逻辑删除） | 无 | `R<Void>` |


**CommentVO（管理端视图）：**

```java
public class CommentVO {
    private Long id;
    private Long postId;
    private String postTitle;       // 关联帖子标题（截断50字）
    private String authorName;      // 评论用户昵称
    private Long authorId;
    private Long parentId;          // 父评论ID（null=顶级评论）
    private String content;
    private Integer likes;
    private Integer status;         // 0=正常, 1=已删除
    private LocalDateTime createdTime;
}
```

**实现要点：**

+ 列表查询关联帖子表获取 postTitle，关联用户表获取 authorName
+ keyword 搜索评论内容
+ 逻辑删除：`UPDATE comments SET status = 1 WHERE id = ?`

---

#### 2.2.5 话题管理接口
| 方法 | 路径 | 说明 | 请求参数 | 返回数据 |
| --- | --- | --- | --- | --- |
| GET | `/api/admin/topics` | 分页查询话题列表 | `?page=1&size=10&keyword=` | `PageResult<TopicVO>` |
| POST | `/api/admin/topics` | 创建话题 | Body: `TopicCreateDTO` | `R<TopicVO>` |
| PUT | `/api/admin/topics/{id}` | 修改话题 | Body: `TopicUpdateDTO` | `R<TopicVO>` |
| DELETE | `/api/admin/topics/{id}` | 删除话题 | 无 | `R<Void>` |


**TopicVO：**

```java
public class TopicVO {
    private Long id;
    private String title;
    private String description;
    private Integer viewCount;
    private Integer postCount;
    private LocalDateTime createdTime;
    private LocalDateTime updatedTime;
}
```

**TopicCreateDTO：**

```java
public class TopicCreateDTO {
    @NotBlank(message = "话题标题不能为空")
    @Size(max = 50, message = "话题标题最多50字")
    private String title;

    @Size(max = 200, message = "话题描述最多200字")
    private String description;
}
```

**TopicUpdateDTO：**

```java
public class TopicUpdateDTO {
    @NotBlank(message = "话题标题不能为空")
    @Size(max = 50, message = "话题标题最多50字")
    private String title;

    @Size(max = 200, message = "话题描述最多200字")
    private String description;
}
```

**实现要点：**

+ 创建时检查 title 是否重复
+ 删除话题时，可选择是否同时删除该话题下的帖子（提供 `?cascade=true` 参数，默认 false）
+ 删除话题：物理删除（`DELETE FROM topics WHERE id = ?`），因为话题表没有 status 字段

---

#### 2.2.6 资讯管理接口
| 方法 | 路径 | 说明 | 请求参数 | 返回数据 |
| --- | --- | --- | --- | --- |
| GET | `/api/admin/news` | 分页查询资讯列表 | `?page=1&size=10&keyword=&category=` | `PageResult<NewsVO>` |
| POST | `/api/admin/news` | 创建资讯 | Body: `NewsCreateDTO` | `R<NewsVO>` |
| PUT | `/api/admin/news/{id}` | 修改资讯 | Body: `NewsUpdateDTO` | `R<NewsVO>` |
| DELETE | `/api/admin/news/{id}` | 删除资讯 | 无 | `R<Void>` |


**NewsVO：**

```java
public class NewsVO {
    private Long id;
    private String title;
    private String summary;
    private String content;
    private String coverUrl;
    private String author;
    private String tags;            // 逗号分隔
    private String category;
    private Integer likeCount;
    private Integer commentCount;
    private Integer collectCount;
    private LocalDateTime createdTime;
    private LocalDateTime updatedTime;
}
```

**NewsCreateDTO：**

```java
public class NewsCreateDTO {
    @NotBlank(message = "标题不能为空")
    @Size(max = 100, message = "标题最多100字")
    private String title;

    private String summary;
    private String content;
    private String coverUrl;
    private String author;

    @Size(max = 200, message = "标签最多200字")
    private String tags;

    private String category;
}
```

**NewsUpdateDTO：** 与 NewsCreateDTO 字段相同，所有字段可选。

**实现要点：**

+ 资讯创建时自动填充 createdTime/updatedTime
+ 删除资讯：物理删除（`DELETE FROM news WHERE id = ?`）

---

### 2.3 后端代码文件清单
在 `soccer-forum-service/src/main/java/com/soccer/forum/service/modules/admin/` 下新增以下文件：

```plain
admin/
├── controller/
│   ├── AdminDashboardController.java
│   ├── AdminUserController.java
│   ├── AdminPostController.java
│   ├── AdminCommentController.java
│   ├── AdminTopicController.java
│   └── AdminNewsController.java
├── service/
│   ├── AdminDashboardService.java
│   ├── AdminUserService.java
│   ├── AdminPostService.java
│   ├── AdminCommentService.java
│   ├── AdminTopicService.java
│   └── AdminNewsService.java
├── service/impl/
│   ├── AdminDashboardServiceImpl.java
│   ├── AdminUserServiceImpl.java
│   ├── AdminPostServiceImpl.java
│   ├── AdminCommentServiceImpl.java
│   ├── AdminTopicServiceImpl.java
│   └── AdminNewsServiceImpl.java
├── mapper/
│   ├── AdminDashboardMapper.java
│   ├── AdminUserMapper.java
│   ├── AdminPostMapper.java
│   └── AdminCommentMapper.java
└── dto/
    ├── DashboardStatsVO.java
    ├── DailyTrendVO.java
    ├── UserVO.java
    ├── PostVO.java
    ├── CommentVO.java
    ├── TopicVO.java
    ├── TopicCreateDTO.java
    ├── TopicUpdateDTO.java
    ├── NewsVO.java
    ├── NewsCreateDTO.java
    └── NewsUpdateDTO.java
```

**复用已有 Mapper：** Topic 和 News 的管理端 Service 可直接复用现有的 `TopicMapper` 和 `NewsMapper`，无需新建 Admin 专用 Mapper。

### 2.4 需修改的现有文件
| 文件 | 修改内容 |
| --- | --- |
| `SecurityConfig.java` | 1. 添加 `@EnableMethodSecurity` 注解   2. 新增 `/api/admin/**` 路径规则为 `hasRole("ADMIN")`   3. 管理端登录页面路径 `/admin/**` 放行（静态资源） |


### 2.5 数据库变更
**无需新增表**。所有管理端操作基于现有表：

+ `users` — 用户管理
+ `posts` — 帖子管理
+ `comments` — 评论管理
+ `topics` — 话题管理
+ `news` — 资讯管理

仅需要的 SQL 变更：确认 `topics` 表有 `created_time` 和 `updated_time` 字段。如果没有，需添加：

```sql
ALTER TABLE topics ADD COLUMN created_time DATETIME DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE topics ADD COLUMN updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;
```

---

## 三、前端设计
### 3.1 项目初始化
```bash
# 在 football-AI 根目录下
npm create vite@latest admin-frontend -- --template vue
cd admin-frontend
npm install
npm install element-plus @element-plus/icons-vue vue-router@4 pinia axios
```

### 3.2 目录结构
```plain
admin-frontend/src/
├── api/
│   ├── request.js              # Axios 实例封装（含 JWT 拦截器）
│   ├── dashboard.js            # 数据看板 API
│   ├── user.js                 # 用户管理 API
│   ├── post.js                 # 帖子管理 API
│   ├── comment.js              # 评论管理 API
│   ├── topic.js                # 话题管理 API
│   └── news.js                 # 资讯管理 API
├── assets/
│   └── logo.svg
├── components/
│   └── ImagePreview.vue        # 图片预览组件
├── layout/
│   ├── AdminLayout.vue         # 管理端整体布局（侧边栏 + 顶栏 + 内容区）
│   └── Sidebar.vue             # 侧边栏导航
├── router/
│   └── index.js                # 路由配置（含路由守卫）
├── stores/
│   └── user.js                 # 用户状态（token、角色）
├── utils/
│   └── format.js               # 日期格式化等工具
├── views/
│   ├── login/
│   │   └── LoginView.vue       # 登录页
│   ├── dashboard/
│   │   └── DashboardView.vue   # 数据看板
│   ├── user/
│   │   └── UserView.vue        # 用户管理
│   ├── post/
│   │   └── PostView.vue        # 帖子管理
│   ├── comment/
│   │   └── CommentView.vue     # 评论管理
│   ├── topic/
│   │   └── TopicView.vue       # 话题管理
│   └── news/
│       └── NewsView.vue        # 资讯管理
├── App.vue
└── main.js
```

### 3.3 核心配置文件
#### 3.3.1 `vite.config.js`
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5174,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

#### 3.3.2 `main.js`
```javascript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import zhCn from 'element-plus/dist/locale/zh-cn.mjs'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'
import App from './App.vue'
import router from './router'

const app = createApp(App)

// 注册所有 Element Plus 图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

app.use(createPinia())
app.use(router)
app.use(ElementPlus, { locale: zhCn })
app.mount('#app')
```

#### 3.3.3 `src/api/request.js` — Axios 封装
```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '../router'

const request = axios.create({
  baseURL: '/api',
  timeout: 10000
})

// 请求拦截器：附加 JWT Token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('admin_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器：统一错误处理
request.interceptors.response.use(
  response => {
    const { code, msg, data } = response.data
    if (code === 200) {
      return data
    }
    ElMessage.error(msg || '请求失败')
    return Promise.reject(new Error(msg))
  },
  error => {
    if (error.response?.status === 401 || error.response?.status === 403) {
      ElMessage.error('无权限访问，请重新登录')
      localStorage.removeItem('admin_token')
      router.push('/login')
    } else {
      ElMessage.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default request
```

#### 3.3.4 `src/router/index.js` — 路由配置
```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('../views/login/LoginView.vue'),
    meta: { title: '登录' }
  },
  {
    path: '/',
    component: () => import('../layout/AdminLayout.vue'),
    redirect: '/dashboard',
    meta: { requiresAuth: true },
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('../views/dashboard/DashboardView.vue'),
        meta: { title: '数据看板', icon: 'DataAnalysis' }
      },
      {
        path: 'users',
        name: 'Users',
        component: () => import('../views/user/UserView.vue'),
        meta: { title: '用户管理', icon: 'User' }
      },
      {
        path: 'posts',
        name: 'Posts',
        component: () => import('../views/post/PostView.vue'),
        meta: { title: '帖子管理', icon: 'Document' }
      },
      {
        path: 'comments',
        name: 'Comments',
        component: () => import('../views/comment/CommentView.vue'),
        meta: { title: '评论管理', icon: 'ChatDotRound' }
      },
      {
        path: 'topics',
        name: 'Topics',
        component: () => import('../views/topic/TopicView.vue'),
        meta: { title: '话题管理', icon: 'PriceTag' }
      },
      {
        path: 'news',
        name: 'News',
        component: () => import('../views/news/NewsView.vue'),
        meta: { title: '资讯管理', icon: 'Notification' }
      }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫：未登录跳转登录页
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('admin_token')
  if (to.meta.requiresAuth && !token) {
    next('/login')
  } else if (to.path === '/login' && token) {
    next('/dashboard')
  } else {
    next()
  }
})

export default router
```

### 3.4 布局设计
#### 3.4.1 `AdminLayout.vue` — 整体布局
```plain
┌─────────────────────────────────────────────────────┐
│  顶部导航栏 (60px)                                   │
│  Logo: 绿茵集管理后台    |    管理员名称  |  退出登录   │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│  侧边栏   │          内容区域                         │
│  (200px)  │                                          │
│          │  ┌──────────────────────────────────────┐ │
│  数据看板  │  │  面包屑导航                           │ │
│  用户管理  │  ├──────────────────────────────────────┤ │
│  帖子管理  │  │                                      │ │
│  评论管理  │  │          页面内容                      │ │
│  话题管理  │  │                                      │ │
│  资讯管理  │  │                                      │ │
│          │  └──────────────────────────────────────┘ │
│          │                                          │
└──────────┴──────────────────────────────────────────┘
```

**使用 Element Plus 组件：**

+ `el-container` + `el-aside` + `el-header` + `el-main`
+ 侧边栏：`el-menu`（`collapse` 可折叠）
+ 顶栏：`el-breadcrumb` + 用户信息下拉

### 3.5 页面详细设计
#### 3.5.1 登录页 (`LoginView.vue`)
**功能：**

+ 用户名 + 密码登录
+ 调用 `/api/auth/login` 接口
+ 登录成功后检查 `role === 'ADMIN'`，是则存储 token 并跳转 `/dashboard`，否则提示"无管理员权限"
+ token 存储在 `localStorage` 的 `admin_token` 字段

**UI 布局：**

+ 居中卡片式登录框
+ 背景：足球主题渐变色
+ Element Plus 组件：`el-card` + `el-form` + `el-input` + `el-button`

---

#### 3.5.2 数据看板 (`DashboardView.vue`)
**功能：**

1. 顶部统计卡片（4个）：总用户数、总帖子数、总评论数、总话题数
2. 今日新增行（3个）：今日新增用户、今日新增帖子、今日新增评论
3. 近7天趋势折线图：用户注册趋势、帖子发布趋势、评论趋势

**UI 布局：**

```plain
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│  总用户数  │ │  总帖子数  │ │  总评论数  │ │  总话题数  │
│   1,234   │ │   567    │ │  8,901   │ │    42    │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
┌──────────┐ ┌──────────┐ ┌──────────┐
│ 今日新用户 │ │ 今日新帖子 │ │ 今日新评论 │
│    12     │ │    8     │ │    45    │
└──────────┘ └──────────┘ └──────────┘
┌────────────────────────────────────────────────────┐
│          近7天趋势折线图 (ECharts)                    │
│   — 注册用户  — 新帖  — 新评论                        │
│                                                    │
└────────────────────────────────────────────────────┘
```

**Element Plus 组件：** `el-card` + `el-col`/`el-row` + `el-statistic`  
**图表库：** ECharts 5（`npm install echarts`）

**数据获取：**

+ `GET /api/admin/dashboard/stats` → 统计卡片
+ `GET /api/admin/dashboard/trend?days=7` → 趋势图

---

#### 3.5.3 用户管理 (`UserView.vue`)
**功能：**

1. 搜索栏：关键词搜索（用户名/昵称）、角色筛选、状态筛选
2. 用户列表表格：ID、用户名、昵称、邮箱、角色、状态、注册时间、操作
3. 操作：启用/禁用（切换开关）、修改角色（下拉选择）、删除（确认弹窗）

**UI 布局：**

```plain
┌────────────────────────────────────────────────────┐
│  [关键词输入框]  [角色: 全部▼]  [状态: 全部▼]  [搜索]  │
├────┬──────┬──────┬───────┬──────┬──────┬───────┬─────┤
│ ID │ 用户名 │ 昵称  │  邮箱  │ 角色  │ 状态  │ 注册时间│ 操作│
├────┼──────┼──────┼───────┼──────┼──────┼───────┼─────┤
│ 1  │admin │管理员 │a@b.com│ADMIN │ 正常  │01-15  │ ··· │
│ 2  │zhang3│小张  │z@b.com│USER  │ 正常  │02-20  │ ··· │
├────┴──────┴──────┴───────┴──────┴──────┴───────┴─────┤
│                    < 1 2 3 ... 10 >                   │
└────────────────────────────────────────────────────┘
```

**Element Plus 组件：** `el-table` + `el-pagination` + `el-input` + `el-select` + `el-switch` + `el-popconfirm`

**交互逻辑：**

+ 启用/禁用：`el-switch` 直接调用 `PUT /api/admin/users/{id}/status`
+ 修改角色：点击角色列的 `el-tag`，弹出 `el-select` 选择角色后确认
+ 删除：`el-popconfirm` 确认后调用 `DELETE /api/admin/users/{id}`

---

#### 3.5.4 帖子管理 (`PostView.vue`)
**功能：**

1. 搜索栏：关键词搜索（标题/内容）、状态筛选、话题筛选
2. 帖子列表表格：ID、标题、作者、话题、浏览/点赞/评论数、状态、发布时间、操作
3. 操作：查看详情（弹窗）、显示/隐藏、删除

**UI 布局：**

```plain
┌─────────────────────────────────────────────────────────┐
│  [关键词输入框]  [状态: 全部▼]  [话题: 全部▼]  [搜索]  [重置] │
├────┬──────────┬──────┬──────┬───────┬──────┬──────┬─────┤
│ ID │   标题    │ 作者  │ 话题  │ 浏览/赞│ 状态  │ 时间  │ 操作│
├────┼──────────┼──────┼──────┼───────┼──────┼──────┼─────┤
│ 1  │梅西夺冠... │小张  │赛事  │120/30 │ 正常  │02-20 │ ··· │
└────┴──────────┴──────┴──────┴───────┴──────┴──────┴─────┘
```

**Element Plus 组件：** `el-table` + `el-pagination` + `el-dialog`（查看详情）+ `el-tag` + `el-popconfirm`

**交互逻辑：**

+ 查看详情：点击标题弹出 `el-dialog`，展示完整帖子内容 + 图片
+ 显示/隐藏：`el-switch` 调用 `PUT /api/admin/posts/{id}/status`
+ 删除：`el-popconfirm` 确认后调用 `DELETE /api/admin/posts/{id}`

---

#### 3.5.5 评论管理 (`CommentView.vue`)
**功能：**

1. 搜索栏：关键词搜索（评论内容）、帖子ID筛选
2. 评论列表表格：ID、内容（截断）、所属帖子、评论者、点赞数、状态、时间、操作
3. 操作：删除

**UI 布局：**

```plain
┌──────────────────────────────────────────────────────────┐
│  [关键词输入框]  [帖子ID]  [搜索]  [重置]                    │
├────┬──────────────┬──────────┬──────┬──────┬──────┬──────┤
│ ID │    内容       │ 所属帖子  │ 评论者 │ 点赞  │ 状态  │ 操作 │
├────┼──────────────┼──────────┼──────┼──────┼──────┼──────┤
│ 1  │踢得不错！...   │梅西夺冠.. │小张   │  5   │ 正常  │ 删除 │
└────┴──────────────┴──────────┴──────┴──────┴──────┴──────┘
```

**Element Plus 组件：** `el-table` + `el-pagination` + `el-popconfirm`

---

#### 3.5.6 话题管理 (`TopicView.vue`)
**功能：**

1. 搜索栏：关键词搜索（标题）
2. 话题列表表格：ID、标题、描述、浏览数、帖子数、创建时间、操作
3. 操作：新增（弹窗表单）、编辑（弹窗表单）、删除
4. 新增/编辑弹窗：标题（必填，最多50字）、描述（选填，最多200字）

**UI 布局：**

```plain
┌──────────────────────────────────────────────────────────┐
│  [关键词输入框]  [搜索]  [重置]              [+ 新增话题]   │
├────┬──────────┬──────────┬──────┬──────┬──────┬──────────┤
│ ID │   标题    │   描述    │ 浏览数│ 帖子数│ 时间  │   操作   │
├────┼──────────┼──────────┼──────┼──────┼──────┼──────────┤
│ 1  │  赛事讨论  │ 比赛相关... │ 1200 │  56  │01-10 │ 编辑 删除│
└────┴──────────┴──────────┴──────┴──────┴──────┴──────────┘

新增/编辑弹窗：
┌──────────────────────────┐
│  新增话题 / 编辑话题       │
│                          │
│  话题标题: [___________]  │
│  话题描述: [___________]  │
│                          │
│       [取消]  [确定]      │
└──────────────────────────┘
```

**Element Plus 组件：** `el-table` + `el-pagination` + `el-dialog` + `el-form` + `el-popconfirm`

**交互逻辑：**

+ 新增：点击"新增话题"→ 弹窗 → 填写表单 → `POST /api/admin/topics`
+ 编辑：点击"编辑"→ 弹窗回填数据 → 修改 → `PUT /api/admin/topics/{id}`
+ 删除：`el-popconfirm` 确认 → `DELETE /api/admin/topics/{id}`

---

#### 3.5.7 资讯管理 (`NewsView.vue`)
**功能：**

1. 搜索栏：关键词搜索（标题）、分类筛选
2. 资讯列表表格：ID、标题、作者、分类、标签、点赞/评论/收藏数、时间、操作
3. 操作：新增（弹窗表单）、编辑（弹窗表单）、删除
4. 新增/编辑弹窗：标题（必填）、摘要、内容（富文本编辑器）、封面图URL、作者、标签、分类

**UI 布局：**

```plain
┌──────────────────────────────────────────────────────────────┐
│  [关键词输入框]  [分类: 全部▼]  [搜索]  [重置]    [+ 新增资讯]   │
├────┬──────────┬──────┬──────┬──────┬────────────┬────────────┤
│ ID │   标题    │ 作者  │ 分类  │ 标签  │ 赞/评/藏    │   操作     │
├────┼──────────┼──────┼──────┼──────┼────────────┼────────────┤
│ 1  │转会汇总.. │管理员 │转会   │英超, │ 30/12/8   │ 编辑 删除  │
└────┴──────────┴──────┴──────┴──────┴────────────┴────────────┘

新增/编辑弹窗：
┌──────────────────────────────────────┐
│  新增资讯 / 编辑资讯                   │
│                                      │
│  标题:    [________________________]  │
│  摘要:    [________________________]  │
│  封面URL: [________________________]  │
│  作者:    [________________________]  │
│  分类:    [________________________]  │
│  标签:    [________________________]  │
│  内容:                                 │
│  ┌──────────────────────────────────┐ │
│  │  富文本编辑器区域                  │ │
│  │  (使用 el-input type="textarea"  │ │
│  │   或集成 wangeditor)             │ │
│  └──────────────────────────────────┘ │
│                                      │
│            [取消]  [确定]              │
└──────────────────────────────────────┘
```

**Element Plus 组件：** `el-table` + `el-pagination` + `el-dialog` + `el-form` + `el-input` + `el-select` + `el-popconfirm`

**富文本编辑器（可选）：**

+ 简化方案：使用 `el-input type="textarea"` 配合 `:rows="10"`，内容为 Markdown 或纯文本
+ 完整方案：集成 `@wangeditor/editor-for-vue`（需额外安装）

---

### 3.6 API 请求封装示例
#### `src/api/dashboard.js`
```javascript
import request from './request'

export const getDashboardStats = () => request.get('/admin/dashboard/stats')
export const getDashboardTrend = (days = 7) => request.get('/admin/dashboard/trend', { params: { days } })
```

#### `src/api/user.js`
```javascript
import request from './request'

export const getUserList = (params) => request.get('/admin/users', { params })
export const updateUserStatus = (id, status) => request.put(`/admin/users/${id}/status`, { status })
export const updateUserRole = (id, role) => request.put(`/admin/users/${id}/role`, { role })
export const deleteUser = (id) => request.delete(`/admin/users/${id}`)
```

#### `src/api/post.js`
```javascript
import request from './request'

export const getPostList = (params) => request.get('/admin/posts', { params })
export const updatePostStatus = (id, status) => request.put(`/admin/posts/${id}/status`, { status })
export const deletePost = (id) => request.delete(`/admin/posts/${id}`)
```

#### `src/api/comment.js`
```javascript
import request from './request'

export const getCommentList = (params) => request.get('/admin/comments', { params })
export const deleteComment = (id) => request.delete(`/admin/comments/${id}`)
```

#### `src/api/topic.js`
```javascript
import request from './request'

export const getTopicList = (params) => request.get('/admin/topics', { params })
export const createTopic = (data) => request.post('/admin/topics', data)
export const updateTopic = (id, data) => request.put(`/admin/topics/${id}`, data)
export const deleteTopic = (id) => request.delete(`/admin/topics/${id}`)
```

#### `src/api/news.js`
```javascript
import request from './request'

export const getNewsList = (params) => request.get('/admin/news', { params })
export const createNews = (data) => request.post('/admin/news', data)
export const updateNews = (id, data) => requestNews.put(`/admin/news/${id}`, data)
export const deleteNews = (id) => request.delete(`/admin/news/${id}`)
```

---

## 四、完整复现步骤
以下步骤可在另一台电脑上通过 Trae 逐步输入，完整复现整个后台管理系统。

### 步骤 1：后端 — 新增管理模块
**1.1 修改 SecurityConfig**

文件：`soccer-forum-parent/soccer-forum-service/src/main/java/com/soccer/forum/service/config/SecurityConfig.java`

修改内容：

1. 在类上添加 `@EnableMethodSecurity` 注解
2. 在 `authorizeHttpRequests` 链中，在现有规则之前添加：

```java
.requestMatchers("/api/admin/**").hasRole("ADMIN")
```

**1.2 创建 DTO/VO 类**

在 `modules/admin/dto/` 下创建以下 11 个类（字段详见 2.2 节）：

+ `DashboardStatsVO`
+ `DailyTrendVO`
+ `UserVO`
+ `PostVO`
+ `CommentVO`
+ `TopicVO`
+ `TopicCreateDTO`
+ `TopicUpdateDTO`
+ `NewsVO`
+ `NewsCreateDTO`
+ `NewsUpdateDTO`

**1.3 创建 Mapper 接口**

在 `modules/admin/mapper/` 下创建（仅需要自定义 SQL 的）：

+ `AdminDashboardMapper.java` — 统计查询
+ `AdminUserMapper.java` — 用户管理查询（含 keyword 模糊搜索）
+ `AdminPostMapper.java` — 帖子管理查询（含关联用户名、话题名）
+ `AdminCommentMapper.java` — 评论管理查询（含关联帖子标题、用户名）

对于 Topic 和 News，直接复用现有 `TopicMapper` 和 `NewsMapper`。

**Mapper XML 示例（AdminDashboardMapper.xml）：**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soccer.forum.service.modules.admin.mapper.AdminDashboardMapper">

    <select id="countActiveUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM users WHERE status = 1
    </select>
    <select id="countActivePosts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM posts WHERE status = 0
    </select>
    <select id="countComments" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comments WHERE status = 0
    </select>
    <select id="countTopics" resultType="java.lang.Long">
        SELECT COUNT(*) FROM topics
    </select>
    <select id="countTodayNewUsers" resultType="java.lang.Long">
        SELECT COUNT(*) FROM users
        WHERE DATE(created_time) = CURDATE() AND status = 1
    </select>
    <select id="countTodayNewPosts" resultType="java.lang.Long">
        SELECT COUNT(*) FROM posts
        WHERE DATE(created_time) = CURDATE() AND status = 0
    </select>
    <select id="countTodayNewComments" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comments
        WHERE DATE(created_time) = CURDATE() AND status = 0
    </select>
    <select id="getDailyTrend" resultType="com.soccer.forum.service.modules.admin.dto.DailyTrendVO">
        SELECT
            d.date,
            COALESCE(u.cnt, 0) AS newUsers,
            COALESCE(p.cnt, 0) AS newPosts,
            COALESCE(c.cnt, 0) AS newComments
        FROM (
            SELECT DATE_SUB(CURDATE(), INTERVAL n DAY) AS date
            FROM (
                SELECT 0 AS n UNION SELECT 1 UNION SELECT 2 UNION SELECT 3
                UNION SELECT 4 UNION SELECT 5 UNION SELECT 6
            ) nums
        ) d
        LEFT JOIN (SELECT DATE(created_time) AS dt, COUNT(*) AS cnt FROM users WHERE status = 1 GROUP BY dt) u ON d.date = u.dt
        LEFT JOIN (SELECT DATE(created_time) AS dt, COUNT(*) AS cnt FROM posts WHERE status = 0 GROUP BY dt) p ON d.date = p.dt
        LEFT JOIN (SELECT DATE(created_time) AS dt, COUNT(*) AS cnt FROM comments WHERE status = 0 GROUP BY dt) c ON d.date = c.dt
        ORDER BY d.date ASC
        LIMIT #{days}
    </select>
</mapper>

```

**1.4 创建 Service 接口和实现类**

在 `modules/admin/service/` 和 `modules/admin/service/impl/` 下创建 6 组 Service：

每个 Service 注入对应的 Mapper（或复用现有 Mapper），实现分页查询、CRUD 操作。关键逻辑：

+ **AdminDashboardServiceImpl**：调用 AdminDashboardMapper 的统计方法，组装 DashboardStatsVO 和趋势数据
+ **AdminUserServiceImpl**：分页查询 + keyword 模糊搜索 + 状态/角色修改
+ **AdminPostServiceImpl**：分页查询 + 关联查询作者昵称和话题名称 + 状态修改 + 逻辑删除
+ **AdminCommentServiceImpl**：分页查询 + 关联查询帖子标题和用户昵称 + 逻辑删除
+ **AdminTopicServiceImpl**：CRUD + 创建时标题去重
+ **AdminNewsServiceImpl**：CRUD

**1.5 创建 Controller**

在 `modules/admin/controller/` 下创建 6 个 Controller，每个类添加 `@RestController` + `@RequestMapping("/api/admin/...")` + `@PreAuthorize("hasRole('ADMIN')")`。

---

### 步骤 2：前端 — 创建管理端项目
**2.1 初始化项目**

```bash
cd /path/to/football-AI
npm create vite@latest admin-frontend -- --template vue
cd admin-frontend
npm install element-plus @element-plus/icons-vue vue-router@4 pinia axios echarts
```

**2.2 配置文件**

按照 3.3 节创建/修改以下文件：

+ `vite.config.js` — 配置代理
+ `src/main.js` — 注册 Element Plus + Pinia + Router + 图标
+ `src/api/request.js` — Axios 封装 + JWT 拦截器
+ `src/router/index.js` — 路由配置 + 路由守卫

**2.3 布局和公共组件**

+ `src/layout/AdminLayout.vue` — 整体布局（侧边栏 + 顶栏 + 内容区）
+ `src/layout/Sidebar.vue` — 侧边栏导航菜单
+ `src/stores/user.js` — Pinia 用户状态管理
+ `src/utils/format.js` — 日期格式化工具

**2.4 页面组件**

按照 3.5 节创建 7 个页面视图：

1. `src/views/login/LoginView.vue`
2. `src/views/dashboard/DashboardView.vue`
3. `src/views/user/UserView.vue`
4. `src/views/post/PostView.vue`
5. `src/views/comment/CommentView.vue`
6. `src/views/topic/TopicView.vue`
7. `src/views/news/NewsView.vue`

**2.5 API 封装**

按照 3.6 节创建 6 个 API 文件：

+ `src/api/dashboard.js`
+ `src/api/user.js`
+ `src/api/post.js`
+ `src/api/comment.js`
+ `src/api/topic.js`
+ `src/api/news.js`

**2.6 启动验证**

```bash
cd admin-frontend
npm run dev
# 访问 http://localhost:5174
# 使用 admin / 123456 登录
```

---

### 步骤 3：数据库检查
执行以下 SQL 确认表结构完整：

```sql
-- 确认 topics 表有 created_time 和 updated_time
DESCRIBE topics;

-- 如果缺少时间字段，执行：
ALTER TABLE topics ADD COLUMN created_time DATETIME DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE topics ADD COLUMN updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;

-- 确认 admin 种子用户存在
SELECT * FROM users WHERE username = 'admin';
-- 如果不存在，插入：
-- INSERT INTO users (username, password, nickname, role, status) VALUES ('admin', '$2a$10$...(bcrypt of 123456)', '管理员', 'ADMIN', 1);
```

---

## 五、接口汇总表
| 模块 | 方法 | 路径 | 说明 | 需要ADMIN |
| --- | --- | --- | --- | --- |
| 看板 | GET | /api/admin/dashboard/stats | 核心统计 | 是 |
| 看板 | GET | /api/admin/dashboard/trend | 趋势数据 | 是 |
| 用户 | GET | /api/admin/users | 用户列表 | 是 |
| 用户 | PUT | /api/admin/users/{id}/status | 修改状态 | 是 |
| 用户 | PUT | /api/admin/users/{id}/role | 修改角色 | 是 |
| 用户 | DELETE | /api/admin/users/{id} | 删除用户 | 是 |
| 帖子 | GET | /api/admin/posts | 帖子列表 | 是 |
| 帖子 | PUT | /api/admin/posts/{id}/status | 显示/隐藏 | 是 |
| 帖子 | DELETE | /api/admin/posts/{id} | 删除帖子 | 是 |
| 评论 | GET | /api/admin/comments | 评论列表 | 是 |
| 评论 | DELETE | /api/admin/comments/{id} | 删除评论 | 是 |
| 话题 | GET | /api/admin/topics | 话题列表 | 是 |
| 话题 | POST | /api/admin/topics | 创建话题 | 是 |
| 话题 | PUT | /api/admin/topics/{id} | 修改话题 | 是 |
| 话题 | DELETE | /api/admin/topics/{id} | 删除话题 | 是 |
| 资讯 | GET | /api/admin/news | 资讯列表 | 是 |
| 资讯 | POST | /api/admin/news | 创建资讯 | 是 |
| 资讯 | PUT | /api/admin/news/{id} | 修改资讯 | 是 |
| 资讯 | DELETE | /api/admin/news/{id} | 删除资讯 | 是 |


**共计 19 个接口**

---

## 六、依赖清单
### 后端（无需新增依赖）
所有后端功能均基于现有 Spring Boot 3.2.5 + MyBatis-Plus + Spring Security + JWT 实现，无需引入新依赖。

### 前端（新增依赖）
```json
{
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.7",
    "element-plus": "^2.7.0",
    "@element-plus/icons-vue": "^2.3.1",
    "axios": "^1.6.0",
    "echarts": "^5.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.2.8"
  }
}
```

---

## 七、注意事项
1. **CORS 配置**：后端需在 SecurityConfig 或 CORS 配置中允许 `http://localhost:5174` 的跨域请求
2. **JWT Token 存储**：管理端使用 `admin_token` 作为 localStorage key，与用户端 `token` 互不干扰
3. **逻辑删除**：用户、帖子、评论使用 status 字段标记删除，不物理删除；话题和资讯使用物理删除
4. **分页参数**：统一使用 `page`（从1开始）和 `size`（默认10），后端返回 `PageResult<T>` 包含 `records`、`total`、`page`、`size`
5. **日期格式**：后端统一返回 `yyyy-MM-dd HH:mm:ss` 格式，前端使用 dayjs 或自定义格式化
6. **错误处理**：后端统一通过 `R<T>` 返回错误码和消息，前端 Axios 拦截器统一处理
7. **富文本编辑器**：资讯管理的编辑器可先用 `el-input textarea` 实现，后续按需升级
